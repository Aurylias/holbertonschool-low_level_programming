#include "main.h"

/**
 * print_rev - Print a string in reverse
 * @s: Pointer to a string
 * Return: None
 */

void print_rev(char *s)
{
	int i;

	while (*last++)
	{
	}

	for (; i >= 0; i--)
	{
		_putchar(s[i]);
	}
	_putchar(s[0]);
}
